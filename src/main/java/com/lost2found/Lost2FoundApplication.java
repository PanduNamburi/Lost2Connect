package com.lost2found;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main Entry Point for Lost2Found - Lost & Found Smart Network Application (Firebase Firestore Backend).
 */
@SpringBootApplication
public class Lost2FoundApplication {

    public static void main(String[] args) {
        SpringApplication.run(Lost2FoundApplication.class, args);
    }
}
